Pseudo Code

What is needed for a countdown?
- Target time (date, specific amt of time, etc.)
- Units (Days, Hours, Minutes, Seconds)

Input: end date compared to the current date/time
Output: Tracker of time by the second
    - Seconds <60
    - Minutes <60
    - Hours <24
    - if (days <=30)

countDownTimer(dt){
    var end = new Date(dt);

    var seconds = 1000;
    var minutes = 
    var hours = 
    var days = 
}